// Closures é armazenar a execução da função para seu retorno ser mas rapido
// salvei a execução na const like assim toda vez que executar like executo 
// apenas o resultado ao envez de executar a função

function exibir(text){
    console.log(text);
}

const like = exibir("like");

like()